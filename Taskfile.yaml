version: 3

env:
  GOEXPERIMENT: runtimesecret

includes:
  shared:
    taskfile: hack/common/Taskfile_service.yaml
    flatten: true
    excludes: [] # put task names in here which are overwritten in this file
    vars:
      CODE_DIRS: "{{.ROOT_DIR}}/cli/... {{.ROOT_DIR}}/internal/... {{.ROOT_DIR}}/pkg/..."
      COMPONENTS: 'krypton tenant-manager'
      CHART_COMPONENTS: 'krypton'
